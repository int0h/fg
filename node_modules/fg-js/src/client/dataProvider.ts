"use strict";

export type Path = string[];

export interface DataScheme {
    [key: string]: Path
};

export class DataProvider {
    private dataScheme: DataScheme;

    constructor (dataScheme: DataScheme){
        this.dataScheme = dataScheme;
    };

   assign(data: any){

   };

    

};